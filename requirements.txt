# Core dependencies
pandas>=1.5
tqdm>=4.60

# For plots (plots/plots.py)
matplotlib>=3.5
seaborn>=0.12
numpy>=1.22
datasets>=2.0
transformers>=4.30

# System dependency (not pip-installable):
# SWI-Prolog must be installed separately; the `swipl` binary must be on PATH.
# Ubuntu/Debian: sudo apt-get install swi-prolog
# macOS:         brew install swi-prolog
